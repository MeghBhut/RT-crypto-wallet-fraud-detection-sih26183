# Copy this file to ".env" and fill in your real key.
# The .env file is git-ignored, so your real key never gets pushed.
#
# Get a free TronGrid API key at: https://www.trongrid.io/dashboard
# (the script also runs WITHOUT a key, just with lower rate limits)

TRONGRID_API_KEY=your_key_here
